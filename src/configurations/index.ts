import { API_ENDPOINT } from '@env'

const storageKey = {
    auth: 'AUTH',
    cart: 'CART',
}

const screenPath = {
    LOGIN: 'Login',
}

export default {
    API_ENDPOINT,
    storageKey,
    screenPath
}